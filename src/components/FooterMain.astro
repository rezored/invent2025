---
import { getLangFromUrl, useTranslations } from "../localization/utils";
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const currentYear = new Date().getFullYear();
---

<footer class="mt-20 border-t border-border-dark pt-8">
    <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-slate-500 text-xs font-mono">
            © <span id="current-year">{currentYear}</span> Invent2025 Ltd. {
                t("footer.rights")
            }
        </p>
        <div class="flex items-center gap-4">
            <div class="flex items-center gap-2">
                <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"
                ></span>
                <span class="text-xs font-mono text-slate-400"
                    >ALL SYSTEMS OPERATIONAL</span
                >
            </div>
            <span class="text-xs font-mono text-slate-600">v2.4.0</span>
        </div>
    </div>
</footer>
